from datetime import timedelta
t = timedelta(days=12, hours=4, seconds=46, microseconds=326) #asigning value to a mew variable, and data to timedelta(days,hours,seconds, microseconds)ALWAYS SINTAXIS LIKE THIS !!!!(NAME=
print("TIME IN SECONDS IS... ", t.total_seconds())#printing what we want, time in seconds
